from django.shortcuts import render
from App01Menus.models import *

# Create your views here.

# def home(request):
#      qr = Website.objects.get(pk=1)
#      return render(request, 'App01Menus:home', locals())
